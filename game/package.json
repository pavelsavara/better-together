{
    "name": "better-together-game",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Better Together tournament engine + shared match core (TS / Node + browser).",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "node --experimental-wasm-jspi --test \"src/**/*.test.ts\"",
        "test:core": "node --test \"src/core/**/*.test.ts\""
    },
    "dependencies": {
        "@pavelsavara/jsco": "file:../../jsco/dist/debug"
    },
    "devDependencies": {
        "typescript": "^5.7.2",
        "@types/node": "^22.10.0"
    }
}